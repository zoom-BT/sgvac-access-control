sgvac:
  jwt:
    secret: "sgvac-test-secret-key-min-32-characters!!"
    expiration-ms: 3600000
  cors:
    allowed-origins: http://localhost:3000

spring:
  datasource:
    url: jdbc:h2:mem:sgvac-test;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
